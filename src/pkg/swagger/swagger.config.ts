import { SwaggerConfig } from './swagger.interface';

/**
 * Configuration for the swagger UI (found at /api/docs).
 * Change this to suit your app!
 */
export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'Sendwave Backend',
  description: 'The API specification for the Sendwave service',
  version: '1.0',
  tags: [],
};
